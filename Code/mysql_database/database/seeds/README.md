# Seeds

UDM_10 không cần dữ liệu seed để chạy. Thư mục này chỉ dành cho dữ liệu demo
không nhạy cảm nếu được bổ sung sau; không đặt database dump, metadata upload
thật, đường dẫn nội bộ hoặc thông tin người dùng tại đây.
