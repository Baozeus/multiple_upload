# Bằng chứng TC_3, TC_4, TC_9, TC_10, TC_12

## Lệnh kiểm chứng tổng

```powershell
cd D:\UDM\UDM_FIXED
$env:QT_QPA_PLATFORM='offscreen'
D:\UDM\.venv\Scripts\python.exe -B -m unittest discover -s Code\tests -v
```

Kết quả mới nhất: `Ran 51 tests ... OK` trong `unit-integration-ui-tests.log`.

## Traceability

| TC | Assertion chính | Test |
|---|---|---|
| TC_3 | 6 source; wave đầu đúng 3 item đầu; wave sau đúng 3 item cuối; 6 SHA-256 trùng | `test_tc3_tc10_six_real_uploads_run_in_fifo_waves_of_three` |
| TC_4 | Queue chỉ chứa 6/8; rejected đúng 2 tên cuối; batch sau thêm lại được; notification đúng | `test_rejected_overflow_never_enters_queue_and_can_be_added_next_time`; `test_eight_file_batch_reports_exact_six_accepted_two_rejected_message` |
| TC_9 | Progress có mốc trung gian, tăng không giảm, 0–100, speed hữu hạn; callback chờ ACK xảy ra trước `SUCCESS` | `test_progress_is_monotonic_and_waits_for_server_ack_before_success`; `test_payload_sent_stays_uploading_until_server_ack` |
| TC_10 | Health-check quan sát `active_uploads=3`, `upload_limit=3`; mọi mẫu client ≤3; client thứ tư bị từ chối; lỗi giải phóng slot | Hai test `test_tc3_tc10_*`, `test_tc10_failed_upload_releases_slot_and_next_file_completes` |
| TC_12 | Port thật khác 9000; health/upload cùng endpoint; SHA-256 trùng; port đóng báo lỗi; endpoint khóa khi queue chạy | `test_tc12_alternate_port_health_upload_and_wrong_endpoint`; UI endpoint-lock test |

Server chậm và ACK trễ chỉ là lớp test. Source production không có sleep hoặc progress giả.

## Artifact

- `unit-integration-ui-tests.log`: toàn bộ test logic, TCP và GUI.
- `mysql-package-tests.log`: package MySQL không kết nối DB thật.
- `test-results.json`: thời gian, môi trường, exit code.
- `ui-evidence.json`: backend Windows và danh sách ảnh.
- `gui-1120x700.png`, `gui-1366x768.png`, `gui-1920x1080.png`: bố cục thật bằng font Windows.
- `gui-duplicate-dialog.png`: dialog xử lý trùng tên.
